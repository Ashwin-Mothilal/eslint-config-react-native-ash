# eslint-config-react-native-ash
My eslint configuration for React Native App.

## Installation

```
yarn add eslint-config-react-native-ash -D
```

## Usage

In `.eslintrc` configuration file

```json
{ 
  "extends": "react-native-ash"
} 
```
